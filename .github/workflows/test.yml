name: test
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate .plugin/plugin.json
        run: node -e "JSON.parse(require('fs').readFileSync('.plugin/plugin.json'))"

      - name: Validate .claude-plugin/plugin.json
        run: node -e "JSON.parse(require('fs').readFileSync('.claude-plugin/plugin.json'))"

      # `.plugin/` is what Copilot CLI reads; `.claude-plugin/` is what Claude Code
      # reads. Every commit that has touched either file has kept them
      # byte-identical, so that is the invariant actually being held -- but the
      # only part of it checked until now was `version`, via plan-release.js
      # --check. That left `description` unguarded, and description is not
      # cosmetic: the marketplace builds its catalog entry from `.plugin/` alone
      # (chibayuki-private-marketplace CHARTER.md §5.5), so a drifted
      # `.claude-plugin/` description would reach Claude Code users while the
      # catalog faithfully mirrored the other file, with nothing comparing them.
      - name: Verify the two plugin manifests are identical
        run: |
          if ! cmp -s .plugin/plugin.json .claude-plugin/plugin.json; then
            echo "::error::.plugin/plugin.json and .claude-plugin/plugin.json differ; they must stay byte-identical"
            diff -u .plugin/plugin.json .claude-plugin/plugin.json || true
            exit 1
          fi
          echo "Both manifests are byte-identical"

      - name: Verify versions and changelog match
        run: node tools/plan-release.js --check

      - name: Test release tooling offline
        run: node --test tools/plan-release.test.js tools/release.test.js

      - name: Verify no local-assets in repo
        run: |
          if find . -type d -name '.local-assets' -not -path './node_modules/*' | grep -q .; then
            echo "::error::Found .local-assets/ in repo — these are personal caches of Microsoft icon packs and must not be committed (licensing). Fix the .gitignore and remove."
            exit 1
          fi
          echo "OK — no .local-assets/ in repo"

      - name: Verify no diagram-cache in repo
        run: |
          if find . -type d -name '.diagram-cache' -not -path './node_modules/*' | grep -q .; then
            echo "::error::Found .diagram-cache/ in repo — these are build artifacts and must not be committed"
            exit 1
          fi
          echo "OK — no .diagram-cache/ in repo"

      - name: Verify resolver rules are valid JSON
        run: node -e "JSON.parse(require('fs').readFileSync('skills/diagram-renderer/assets/icons/resolver-rules.json'))"

      - name: Verify aliases are valid JSON
        run: node -e "JSON.parse(require('fs').readFileSync('skills/diagram-renderer/assets/icons/aliases.json'))"

      - name: Install and run unit tests
        run: |
          cd skills/diagram-renderer
          npm install
          npm test

  e2e:
    name: end-to-end render
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: skills/diagram-renderer
        run: npm install

      # Octicons are MIT and fetched automatically, so CI can populate a real
      # mirror. The Microsoft packs require manual download and are never
      # redistributed, so the committed sample deliberately uses Octicons only.
      - name: Populate icon mirror (Octicons only)
        working-directory: skills/diagram-renderer
        run: READ_AND_AGREE=1 bash scripts/fetch-icons.sh github

      # Renders the committed samples for real: proves Chromium launches, the
      # mirror resolves live jsDelivr URLs, and that an unresolvable icon is a
      # hard failure rather than a silent network fetch.
      - name: Render samples end-to-end
        working-directory: skills/diagram-renderer
        env:
          E2E_OUT_DIR: ${{ github.workspace }}/e2e-out
        run: npm run test:e2e

      - name: Upload rendered samples
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rendered-samples
          path: e2e-out/
          if-no-files-found: warn
